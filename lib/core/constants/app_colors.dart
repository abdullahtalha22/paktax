import 'package:flutter/material.dart';

class AppColors {
  // ── Dark Theme ─────────────────────────────────────────
  static const Color darkBg       = Color(0xFF0A0A0F);
  static const Color darkSurface  = Color(0xFF13131A);
  static const Color darkElevated = Color(0xFF1C1C26);
  static const Color darkBorder   = Color(0xFF252535);

  // ── Light Theme ────────────────────────────────────────
  static const Color lightBg      = Color(0xFFF2F4FF);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightBorder  = Color(0xFFE4E8FF);

  // ── Feature Card Colors ─────────────────────────────────
  static const Color salaryBlue   = Color(0xFF5B6BFF);
  static const Color salaryBlue2  = Color(0xFF3D4FE0);
  static const Color whtOrange    = Color(0xFFFF5722);
  static const Color whtOrange2   = Color(0xFFE64A19);
  static const Color ptaPurple    = Color(0xFF9C27B0);
  static const Color ptaPurple2   = Color(0xFF7B1FA2);
  static const Color remitTeal    = Color(0xFF00BCD4);
  static const Color remitTeal2   = Color(0xFF0097A7);

  // ── Accent ──────────────────────────────────────────────
  static const Color accentGreen  = Color(0xFF00E676);
  static const Color accentGreen2 = Color(0xFF00C853);
  static const Color accentRed    = Color(0xFFFF5252);

  // ── Gradients ───────────────────────────────────────────
  static const List<Color> salaryGrad    = [Color(0xFF5B6BFF), Color(0xFF3D4FE0)];
  static const List<Color> whtGrad       = [Color(0xFFFF5722), Color(0xFFE64A19)];
  static const List<Color> ptaGrad       = [Color(0xFF9C27B0), Color(0xFF7B1FA2)];
  static const List<Color> remitGrad     = [Color(0xFF00BCD4), Color(0xFF0097A7)];
  static const List<Color> greenGrad     = [Color(0xFF00E676), Color(0xFF00C853)];
  static const List<Color> headerGradDk  = [Color(0xFF13131A), Color(0xFF0A0A0F)];
  static const List<Color> headerGradLt  = [Color(0xFFEEF0FF), Color(0xFFF2F4FF)];

  // ── Text ────────────────────────────────────────────────
  static const Color textWhite  = Color(0xFFFFFFFF);
  static const Color textGrey1  = Color(0xFF9E9EB8);
  static const Color textGrey2  = Color(0xFF5C5C7A);
  static const Color textDark   = Color(0xFF0A0A14);
  static const Color textDark2  = Color(0xFF3A3A5C);
}
