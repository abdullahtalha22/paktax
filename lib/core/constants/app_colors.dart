import 'package:flutter/material.dart';

class AppColors {
  // ── Backgrounds ─────────────────────────────────────
  static const Color darkBg       = Color(0xFF07071A);
  static const Color darkSurface  = Color(0xFF0E0E22);
  static const Color darkElevated = Color(0xFF14142E);
  static const Color darkBorder   = Color(0xFF1E1E40);

  static const Color lightBg       = Color(0xFFEEEFFF);
  static const Color lightSurface  = Color(0xFFFFFFFF);
  static const Color lightBorder   = Color(0xFFE0E4FF);
  static const Color lightElevated = Color(0xFFF5F6FF);

  // ── Brand ────────────────────────────────────────────
  static const Color primaryViolet  = Color(0xFF7C5CFC);
  static const Color primaryViolet2 = Color(0xFF5A3DE8);

  // ── Feature Colors ───────────────────────────────────
  static const Color salaryBlue  = Color(0xFF6C8EFF);
  static const Color salaryBlue2 = Color(0xFF4A6CF7);
  static const Color whtOrange   = Color(0xFFFF6B35);
  static const Color whtOrange2  = Color(0xFFE5501A);
  static const Color ptaPurple   = Color(0xFFB06EFF);
  static const Color ptaPurple2  = Color(0xFF8B3FE8);
  static const Color remitTeal   = Color(0xFF00D4B8);
  static const Color remitTeal2  = Color(0xFF00A896);

  // ── Status ───────────────────────────────────────────
  static const Color accentGreen  = Color(0xFF00E5A0);
  static const Color accentGreen2 = Color(0xFF00C882);
  static const Color accentRed    = Color(0xFFFF4C6A);
  static const Color accentRed2   = Color(0xFFE53050);

  // ── Gradients ────────────────────────────────────────
  static const List<Color> primaryGrad = [Color(0xFF9B7FFF), Color(0xFF5A3DE8)];
  static const List<Color> salaryGrad  = [Color(0xFF6C8EFF), Color(0xFF4A6CF7)];
  static const List<Color> whtGrad     = [Color(0xFFFF6B35), Color(0xFFE5501A)];
  static const List<Color> ptaGrad     = [Color(0xFFB06EFF), Color(0xFF8B3FE8)];
  static const List<Color> remitGrad   = [Color(0xFF00D4B8), Color(0xFF00A896)];
  static const List<Color> greenGrad   = [Color(0xFF00E5A0), Color(0xFF00C882)];

  // ── Glass ────────────────────────────────────────────
  static const Color glassDark        = Color(0x18FFFFFF);
  static const Color glassBorderDark  = Color(0x28FFFFFF);
  static const Color glassLight       = Color(0xCCFFFFFF);
  static const Color glassBorderLight = Color(0x80FFFFFF);

  // ── Text ─────────────────────────────────────────────
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textGrey1 = Color(0xFFAAACC8);
  static const Color textGrey2 = Color(0xFF6E709A);
  static const Color textDark  = Color(0xFF0D0D1A);
  static const Color textDark2 = Color(0xFF3A3A5C);
}
